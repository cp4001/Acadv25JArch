# Acadv25JArch

AutoCAD 2025 .NET 플러그인 — 건축/배관 자동화 (급수배관 Tree 분석, 관경 결정, Dia 노트 생성).

## Obsidian
- LineTree 관련: `D:\Dropbox\Obsidian\Projects\LineTree\` (모두 `#AutoCAD` 태그, `LineTree.md` 와 `Cmd_*.excalidraw.md` 는 `#Revit`)
  - `LineTreeTechNote.md` — Tree 분석/관경 결정 기술 노트 (`PipeTreeCon`, `PipeTree`, Supply/Return 모드)
  - `FcuLineTreeTechNote.md` — FCU Tree 분석 (`FCUTREE`, H-W 공식, Leaf tp CrossingWindow, TreeType 스냅샷)
  - `DuctTreeTechNote.md` — Duct Tree 분석 (`DUCTTREE`, CMH 누적, `Total_CMH` XData, Spec 산정 미구현 — 2026-05-14 신규)
  - `Duct_OutLine_Design.md` — **DuctOutline3 설계 사양** (Duct 외곽선 생성 기하 알고리즘 Step 0~4, 측벽+Reducer). **원본은 `C:\Users\junhoi\Desktop\Work\공조덕트사이즈\Duct_OutLine_Design.md`** (사용자 유지) — Obsidian은 복사본. 코드(`DuctOutlineCommand.cs`)는 이 문서를 single source of truth 로 재참조 (2026-06-19 신규)
  - `Duct_OutLine_case2.md` — **Duct_Outline_Case2 설계 사양** (T형 양방향 분기 외곽선 11선 + 하부 캡, Step 1~7). **원본은 `C:\Users\junhoi\Desktop\Work\공조덕트사이즈\Duct_OutLine_case2.md`** (사용자 유지) — Obsidian은 복사본. 코드(`DuctOutlineCase2Command.cs`)는 이 문서를 single source of truth 로 재참조 (2026-06-20 신규)
  - `Duct_OutLine_case1.md` — **Duct_Outline_Case1 설계 사양** (상류 Main aa→하류 Main bb 축소 Reducer + 직각 분기 cc 외곽선 13선, Step 1~9). **원본은 `C:\Users\junhoi\Desktop\Work\공조덕트사이즈\Duct_OutLine_case1.md`** (사용자 유지) — Obsidian은 복사본. 코드(`DuctOutlineCase1Command.cs`)는 이 문서를 single source of truth 로 재참조 (2026-06-21 신규)
  - `Duct_Outline_Elbow.md` — **Duct_Outline_Elbow 설계 사양** (동일폭 90° Elbow 외곽선: Magenta Line 4 + Arc 2 + Green 중심 Arc 1, 총 7개). **원본은 `C:\Users\junhoi\Desktop\Work\공조덕트사이즈\Duct_Outline_Elbow.md`** (사용자 유지) — Obsidian은 복사본. 코드(`DuctOutlineElbowCommand.cs`)는 이 문서를 single source of truth 로 재참조 (2026-06-29 신규)
  - `DuctOutLine_Case_1.md` — **`Duct_C1` 설계 사양** (수직 aa + 교차점 X 좌우 직각 bb/cc → aa 외곽선 Yellow 9개 + bb/cc 각 3분할, v1.4). **원본은 `C:\Users\junhoi\Desktop\Work\건축\Duct-OutLine\DuctOutLine_Case_1.md`** (사용자 유지, 다른 Duct 사양서와 달리 `공조덕트사이즈` 가 아닌 `건축\Duct-OutLine` 폴더) — Obsidian은 복사본. 코드(`DuctC1Command.cs`)는 이 문서를 single source of truth 로 재참조. **`Duct_Outline_Case1`(Reducer+분기, 13선) 과 이름이 유사하나 완전히 다른 형상** — 혼동 주의 (2026-07-03 신규)
  - `Duct_C2.md` — **`Duct_C2` 설계 사양** (aa→bb 직선 축소 리듀서 + 상단 cc 분기 컬러, aa 외곽선 Yellow 10개 + Green 연장 2선 + cc 3분할, v1.5). **원본은 `C:\Users\junhoi\Desktop\Work\건축\Duct-OutLine\Duct_OutLine_Case2.md`** (사용자 유지) — Obsidian 파일명은 **`Duct_C2.md`로 변경**(원본과 다름) — 기존 `Duct_OutLine_case2.md`(구 T형 분기 Case2, 소문자 case2)와 대소문자만 다른 이름이라 Windows 는 동일 파일로 취급함(대소문자 구분 안 함) → 그대로 복사하면 기존 파일을 덮어쓰므로 의도적으로 파일명 분리. 코드(`DuctC2Command.cs`)는 원본 문서를 single source of truth 로 재참조. **`Duct_Outline_Case2`(T형 양방향 분기, 11선) 와 이름이 유사하나 완전히 다른 형상** — 혼동 주의 (2026-07-04 신규)
  - `Duct_Elbow.md` — **`Duct_E` 설계 사양** (직각 엘보: Green측 Line 4 + 외곽 Arc 2 + 중심선 Arc 1, 기준선 Tg/Tr 로 단축, v1.3). **원본은 `C:\Users\junhoi\Desktop\Work\건축\Duct-OutLine\Duct_Elbow.md`** (사용자 유지) — Obsidian은 복사본(파일명 충돌 없음). 코드(`DuctElbowCommand.cs`)는 이 문서를 single source of truth 로 재참조. **`Duct_Outline_Elbow`(동일폭 Arc 2+중심Arc, Magenta) 와 이름이 유사** — 혼동 주의 (2026-07-04 신규)
  - `Duct_End_Elbow.md` — **`Duct_EE` 설계 사양** (말단 상향 분기: a 외곽선 Yellow 6개 + a 연장 1선 + b 3분할, 흐름 방향 순서 선택 a→b, v1.1). **원본은 `C:\Users\junhoi\Desktop\Work\건축\Duct-OutLine\Duct_End_Elbow.md`** (사용자 유지) — Obsidian은 복사본. 코드(`DuctEndElbowCommand.cs`)는 이 문서를 single source of truth 로 재참조 (2026-07-05 신규)
  - `TreeOverrule.md` — `TTG` 커맨드, Tree XData 기반 색상/LineWeight 오버레이 + Block `"Disp"` XData 센터 텍스트
  - `LPM.md` — `LPM` 명령 (Block `"LPM"`/`"Disp"` XData 동시 기록)
  - `CMH.md` — `CMH` 명령 (디퓨져 풍량, LPM 과 동일 패턴, `"CMH"`/`"Disp"` 동시 기록 — 2026-05-14 신규) + `CMHT` (키보드 입력 버전 — 2026-05-18 추가)
  - `pipeFcutree.md` — **BricsCAD V21 이식 사양서** (PipeTree/FcuTree/TTG 전체: 알고리즘·상수·XData·UI·헬퍼·namespace 매핑 단일 자급 문서 — 2026-06-05 신규)
- `Util_Command/` 관련(`CSS`/`ss2`/`To_Duct`/`SCL` 명령) — **원본은 `C:\Users\junhoi\Desktop\Work\Acadv25JArch\Util_Command\`**(솔루션 루트, 사용자 유지) — `CSS.md`(v1.1)/`SS1.md`(v1.2)/`SCL.md`(v1.0). **아직 Obsidian 복사본 없음**(다른 Duct_* 사양서와 달리 이 폴더는 Obsidian 미동기화 상태, 2026-07-08 확인) — 필요 시 동기화 시작 결정 필요. 코드는 `Acadv25JArch/Util_Command.cs`(`CommandUtil` 클래스). 이 폴더에는 `.cs` 참고용 사본도 같이 두는데(`Util_Command\Util_Command.cs`), **실제 소스가 아니라 스냅샷이라 최신화가 밀릴 수 있음** — 진짜 소스는 항상 `Acadv25JArch/Util_Command.cs`.
- DiaNote 관련: `D:\Dropbox\Obsidian\Projects\DiaVerNote\` (모두 `#AutoCAD` 태그)
  - `DiaNote 개요.md` (허브), `DiaTree.md`, `DiaTreeVer.md`, `DiaNoteVer1.md`, `DiaNoteHor.md`, `DiaNoteHor1.md`, `SetDiaNoteBase.md`, `SetDiaNoteBaseText.md`, `DD.md`, `SusPipe.md`
- Architecture(인프라): `D:\Dropbox\Obsidian\Projects\Architecture\` (`#AutoCAD`)
  - `RibbonMenu.md` — JArch 리본 탭 (`Ribbon/CollabRibbon.cs`) 구조·가시성 lifecycle·`SetRibbonTabVisible` / `RefreshDiaNoteHeight` API
  - `AinitCommand.md` — `Ainit` 명령 + `DwgDefaultLoader` (NOD `AINIT_DEFAULTS/DiaNoteHeight` ↔ `DiaNote.BaseLen` 어댑터)
  - `PaletteSample.md` — `SHOWPAL`/`HIDEPAL` 도구 팔레트 (`PaletteSample.cs`), lazy init + 버튼 Tag 기반 `SendStringToExecute`
  - `autocadInstall.md` — **배포 가이드** (Inno Setup 번들 설치 구조, `Assembly.Location` 기반 파일 접근, **빌드 구성(Debug/Release)·네이티브 DLL 의존성**). 원본은 `JArchitecture_Setup\autocadInstall.md`
  - `Architecture.md` — 전체 명령 인덱스, `DiaTreeNote.md`

**노트 태그 규칙**: 각 노트 frontmatter에 `tags: [AutoCAD]` 또는 `tags: [Revit]` 로 플랫폼 구분. 이 프로젝트 관련 노트는 기본 `AutoCAD`, JRevit 관련 노트(`LineTree.md` 등)는 `Revit`.

## QMD 키워드
- Acadv25JArch, AutoCAD plugin, AutoCAD 2025, .NET 8
- LineTree, LineTreeBuilder, LineTreeForm, PipeLoad2, PipeTree, PipeTreeCon (구 LINETREE_FORM/LINETREE)
- FcuLineTree, FcuLineTreeBuilder, FcuLineTreeForm, FCUTREE, FCU
- DuctTree, DuctTreeBuilder, DuctTreeForm, DuctTreeCommand, DUCTTREE, CMH, CMHT, Total_CMH, 디퓨져, 풍량, 공조, Cmd_Block_SetCMH, Cmd_Block_SetCMH_Typed, SetDuctWidth, Cmd_SetDuctWidth, 덕트 폭, TryGetWidthTextByLine, DuctOutline3, DuctOutlineCommand, 덕트 외곽선, 측벽, Reducer, 리듀서, 트라페조이드, Duct_OutLine_Design, Duct_Outline_Case2, DuctOutlineCase2Command, T분기, T형 분기, 하부 캡, Duct_OutLine_case2, Duct_Outline_Case1, DuctOutlineCase1Command, Reducer 30도, 60도, Duct_OutLine_case1, Duct_Outline_Elbow, DuctOutlineElbowCommand, Elbow, 엘보, 90도 Elbow, Arc 외곽선, 내측 Arc, 외측 Arc, 중심 Arc, TryArcAngles, CCW, 접선점
- Duct_C1, DuctC1Command, DuctOutLine_Case_1, 교차점 X, GetClosestPointTo, SplitBranch, XdataCopy, 분기 3분할, 리듀서 끝단 수직, 하부 마감, dirAA, dirBB, dirCC
- Duct_C2, DuctC2Command, Duct_OutLine_Case2, 축소 리듀서, 컬러, takeoff, Green 연장, B1, B2, 30도 사선
- Duct_E, DuctElbowCommand, Duct_Elbow, 엘보, 내측 원호, 외측 원호, 중심선 Arc, 접점 Tg, 접점 Tr, NormalizeAngle
- Duct_EE, DuctEndElbowCommand, Duct_End_Elbow, End Elbow, 말단 상향 분기, 흐름 방향, a1, b1, b2
- TreeOverrule, TreeDrawOverrule, TTG, DrawableOverrule, SetXDataFilter, Disp, BlockReference Disp
- DiaNote, DiaTree, DiaNoteVer, DiaNoteVer1, DiaNoteHor, DiaNoteHor1, SetDiaNoteBase, BaseLen, SusPipe, susDia
- SupplyDiaCalc, ReturnDiaCalc, CalcMode, 관경결정, 관균등표법, 급수배관, 환탕, 순환탕수, 누적체적, H-W 공식, Hazen-Williams
- JXdata, XData, Dia, Tree XData, 15A, Total15A, TotalLPM, LPM, DD command, TotalEquiv
- ArchOverrule, EntityArchc, LayerPalette
- Ribbon, CollabRibbon, JArch tab, RibbonPanel, RibbonRowPanel, RibbonRowBreak, RibbonLabel, SetRibbonTabVisible, RefreshDiaNoteHeight, AINIT_DEFAULTS, Ainit, DwgDefaultLoader, KEY_DiaNoteHeight
- PaletteSample, PaletteHost, PaletteSet, SHOWPAL, HIDEPAL, MyToolsControl, SendStringToExecute, FlowLayoutPanel
- 배포, 인스톨러, Inno Setup, ISCC, JArchitecture_Setup, .iss, ApplicationPlugins, bundle, PackageContents, JArchLicense, PipeLoad, DllImport, P/Invoke, Debug, Release, VCRUNTIME140, ucrtbase, dumpbin, CRT 의존성
- CommandUtil, Util_Command, CSS, DuctPipe_SelectChain, 체인 선택, ss2, MultiLineSplit_v2, InterPointsMLiness2, Base Line 분할, Target 스냅, To_Duct, SetXdataToDuct, EnsureRegApp, SCL, SelectConLine, 진행방향 연속 선택, FindConnectedLines, FindColinearJump, collinear 점프, GAP_MAX

## Build
- TargetFramework: `net8.0-windows8.0`
- 출력 경로: `C:\Jarch25\` (csproj 고정)
- 빌드: `dotnet build Acadv25JArch.csproj -c Debug`
- AutoCAD 2025 로드: `NETLOAD` → `C:\Jarch25\Acadv25JArch.dll` (`DuctSizing.Core.dll` 도 같은 폴더에 자동 배포 — `CopyLocalLockFileAssemblies=true`)
- 주요 패키지: EPPlus 8.3.1, OpenStudio.win-x64 3.10.0, Microsoft.VisualStudio.Services.Client 19.225.1
- **ProjectReference**: `..\DuctSizing.Core\DuctSizing.Core.csproj` (net8.0, `DuctSizingCalculator.ModeD` 등 덕트 사이즈 산정 로직 공유) — 2026-05-19 추가

### 배포 (Inno Setup 번들) — 자세한 가이드: `JArchitecture_Setup\autocadInstall.md`
- 인스톨러: `JArchitecture_Setup\JArchitecture_Setup.iss` → `C:\ProgramData\Autodesk\ApplicationPlugins\JArchitecture.bundle\Contents\` 에 설치 (AutoCAD 자동 로드, `PackageContents.xml`).
- **배포 DLL은 Release 산출물만 사용** — `.iss`의 `Source:` 경로를 `...\Release\...` 로 고정.
- **`JArchLicense.dll`(네이티브 C++ `.vcxproj`)은 반드시 Release\|x64** — Debug 빌드는 재배포 금지된 Debug CRT(`VCRUNTIME140D.dll`/`ucrtbased.dll`)에 링크되어 고객 PC에서 로드 실패(→ "라이선스 확인 오류"). Release CRT(`VCRUNTIME140.dll`)는 AutoCAD/Windows가 제공. 배포 전 `dumpbin /dependents` 로 `D` 접미사 없음 확인.
- `PipeLoad.dll`(관리 .NET `.csproj`)도 Release\|x64 (`BaseOutputPath=C:\Jarch25` → `C:\Jarch25\x64\Release\net8.0-windows\`).
- 빌드: `MSBuild JArchLicense.vcxproj /p:Configuration=Release /p:Platform=x64` · `dotnet build PipeLoad.csproj -c Release -p:Platform=x64` · 인스톨러 `ISCC.exe JArchitecture_Setup.iss`.
- net8.0 `DllImport`는 선언 어셈블리 폴더(`Assembly.Location`)를 탐색하므로 같은 `Contents\` 폴더 co-location만으로 로드 (`SetDllDirectory` 불필요). 단 JArchLicense **자신의** 네이티브 의존성은 OS 로더 기준 → Release CRT 조건 필요.

## Conventions
- **XData RegName은 `"Dia"` 단일화** — 과거 `"DD"`는 사용 금지 (2026-04 통일, DiaNote.cs/LineTreeBuilder.cs 모두 `"Dia"`)
- `[CommandMethod("DD")]`의 `"DD"`는 AutoCAD 명령어 이름 (XData 아님) — 유지
- `JXdata.GetXdata`는 string/double 모두 지원 (2026-04 업데이트)
- WinForms → AutoCAD DB 쓰기 시 반드시 `doc.LockDocument()` (eLockViolation 방지)
- Transaction Commit 후 `node.Line` 사용 금지 → `db.GetObjectId(false, handle, 0)` 재획득
- `JXdata.SetXdata`는 라이선스 만료 시 저장 불가 (`MyPlugin.LicenseDate` 체크)
- **Editor selection(`GetSelection`, `SelectCrossingWindow` 등)은 Transaction 밖에서 호출** — 엔티티 메타만 Pre-Transaction(ForRead)에서 추출 후 Commit → Editor 호출 → Main Transaction으로 분리. FCUTREE 패턴 참고.
- **`SelectCrossingWindow` / `SelectWindow` 호출 전 Zoom fit 필수** — CrossingWindow/Window는 현재 뷰포트 범위 기준이라 대상 엔티티가 뷰 밖이면 매핑 0건. `ZoomExtensionMethods.ZoomToEntities(this Editor, IEnumerable<ObjectId>, double padding=1.1)` 확장 메서드 사용 (`CadFunction.cs`, `namespace AcadFunction`). 빈 시퀀스/`GeometricExtents` 예외 안전 + 기본 5% 패딩. `FCUTREE` Phase 1.5, `LPM` `Cmd_Block_SetLPM` 가 사용. 같은 클래스의 `ZoomObjects` 는 패딩 없이 정확 fit (UCS 변환 사용) — 경계 매칭이 필요 없는 일반 zoom fit 용도.
- **Form → CAD 엔티티 선택 반영**: TreeNode에 `Tag = node.Handle` 저장 → `db.TryGetObjectId(new Handle(long))` → `ed.SetImpliedSelection(new[] { id })` → `doc.Window.Focus()`. Transaction 불필요.
- **DrawableOverrule XData 필터는 `SetXDataFilter` + `IsApplicable`의 `GetXDataForApplication` 2중 검사** — SetXDataFilter만으로는 누수 가능. `TreeOverrule` 패턴 참고.
- **`wd.SubEntityTraits` 변경 시 원복 필수** — Color/LineWeight 등 trait이 다음 Entity 렌더에 누수되면 의도치 않은 시각 효과 발생.
- **Tree/Dia XData 저장은 독립 try-catch** — `FcuLineTreeBuilder.ApplyDiaRecursive` / `LineTreeBuilder.ApplyDiaRecursive` 동일 패턴 (2026-05 LineTree에도 `Tree` XData 기록 적용). 한쪽 실패가 다른 쪽 기록을 막지 않도록. `Tree` 값: FCU는 `node.TreeType`(BuildTree 스냅샷), LineTree는 `node.Type.ToString()` 직접.
- **FcuNode.TreeType 스냅샷 사용** — Apply 시점에 `node.Type.ToString()` 재계산하지 않고 BuildTree 종료시 저장된 `TreeType` 문자열 사용.
- **DiaNote.BaseLen 정적 필드** — `Cmd_DiaNoteVer1` / `Cmd_DiaNoteHor1` 의 `baseDist` 기준 길이. 두 가지 설정 방법: ① `Cmd_SetDiaNoteBase` — 평행 2 Line 수선거리(`l1.StartPoint` → `l2.GetClosestPointTo(p, true)`) 계산. 평행 검사 `Math.Abs(d1.DotProduct(d2)) >= 0.9999`. ② `Cmd_SetDiaNoteBaseText` (2026-05-18) — `PromptDoubleOptions`(기본값 현재 `BaseLen`, 음수/0 금지)로 키보드 입력값 직접 저장. 두 명령 모두 `DwgDefaultLoader.SaveBaseLen` 으로 NOD `AINIT_DEFAULTS/DiaNoteHeight` 영속화. 미설정시 초기값 30.0.
- **LineTreeBuilder.Mode (Supply/Return)** — `PipeTree` 시작 키워드로 선택. Return = 환탕 모드: 모든 Leaf Load 강제 0.105 (XData "15A"·길이 무시), `node.Diameter = ReturnDiaCalc.FromVolume(node.Load)` lookup. Supply는 기존 `SupplyDiaCalc.Calculate2` 균등표법. 근거 표는 `PipeDiaCalc/ALT1. 환탕관경 계산 방식.xlsx` ([§6.5](D:\Dropbox\Obsidian\Projects\LineTree\LineTreeTechNote.md)).
- **`Total15A` XData** (Supply 모드 한정, 2026-05-05) — `SupplyDiaCalc.Calculate2(..., out double total)` 오버로드로 캡처 → `LineNode.TotalEquiv` → `ApplyDiaRecursive` 가 `Mode == Supply && TotalEquiv > 0` 일 때만 `JXdata.SetXdata(line, "Total15A", val.ToString("0.###"))` 기록. 독립 try-catch (Tree/Dia와 분리). `tr.CheckRegName("Total15A")` 등록 필요. Return 모드에서는 의미 없음 → 저장 생략. `TTG` 라벨이 이 값을 읽어 `15[Total15A]-15A`(Leaf) / `<Dia>[<Total15A>]`(Mid/Root) 표시.
- **`TTG` 라벨 우선순위** (`TreeOverrule.DrawLineTree`, 2026-05-19 갱신) — ⓪ **Line `Disp` 존재 → 그 값을 그대로 라벨로 사용 (Duct: `"{a}x{b}"`)** ① `15A` 존재 → `<Dia>[<Total15A>]-<15A>` (LineTree Leaf, 분석 전후 동일 — 분석 전엔 Dia/Total15A가 없어 `"00"` 폴백 표시) ② `15A` 없고 `Total15A` 존재 → `<Dia>[<Total15A>]` (LineTree Mid/Root, Supply 모드) ③ 그 외 → 라벨 없음. `Dia` / `Total15A` 가 비면 `"00"` 으로 폴백 (`?? "00"`). LineTree/FCU 는 Line 에 `Disp` 를 기록하지 않으므로 충돌 없음. **이전(`15[...]` 고정 prefix + `TotalLPM` FCU 폴백)은 주석 처리되어 제거됨** — FCU Mid/Root는 더이상 라벨 표시 안 함. Mid 색상은 ACI 3(Green) — 기존 Blue(5)에서 변경.
- **`TTG` Block `"Disp"` 텍스트** (2026-05-05 추가) — `TreeDrawOverrule` 을 Line용/Block용 두 인스턴스로 분리. Line: `SetXDataFilter("Tree")`. BlockReference: `SetXDataFilter("Disp")` + `IsApplicable` 에서 `GetXDataForApplication("Disp")` 2중 검사. Block 본체는 `base.WorldDraw` 로 정상 렌더 후 `MText` 추가 — `GeometricExtents` 에서 `min(width,height) * 0.5` 를 TextHeight 로, geo 센터(MinPoint+MaxPoint)/2 에 ACI 1(Red) MiddleCenter rotation 0 으로 표시. 단일 클래스 + 생성자 `bool isBlockMode` 분기 (`DrawLineTree` / `DrawBlockDisp`).
- **Block `"Disp"` XData 동시 기록** (2026-05-05 추가) — `Cmd_Block_SetLPM` 가 LPM 값을 저장할 때 `"LPM"` 과 `"Disp"` 양쪽에 같은 문자열 기록. `tr.CheckRegName("Disp")` 등록 필요. `"LPM"` 은 PipeFitting 분석용 원본값, `"Disp"` 는 `TTG` 시각화용 표시값(향후 표시 포맷 분리 가능). `TTG` 토글 시에도 `RegisterRegApp(db, "Disp")` 호출. **`Cmd_Block_SetCMH` 도 동일** — `"CMH"` + `"Disp"` 동시 기록 (2026-05-14).
- **CMH / DUCTTREE 워크플로우** (2026-05-14 추가, 2026-05-20 Mode D + Form 입력 확장) — `Cmd_Block_SetCMH` 가 Block BBox 안 Text 에서 풍량값 추출 → `"CMH"` + `"Disp"` XData 기록 (LPM 과 동일 패턴, 내부 헬퍼 `TryGetLpmByWindowSelect` 공유 — 이름만 LPM, 로직 RegName-agnostic). `DUCTTREE` 명령 (`DuctTreeCommand` / `DuctTreeBuilder`) 이 Leaf Block `"CMH"` 를 부하로 인식, 각 Line 노드에 누적값을 `"Total_CMH"` XData 로 기록 + `"Tree"`(Root/Mid/Leaf) 스냅샷 동시 기록. **`DuctTreeForm` 상단 `pnlMode` (Dock=Top, height 45)** 에 Supply/Return RadioButton + b 최소/최대 `NumericUpDown` 두 개 노출 (Min/Max=100/2000, Increment=50, 기본값 200/500 — `DuctSizing1` Mode D 탭 기본값 일치). Apply 시 `ApplyTotalCmh(node, db, DuctType, bMin, bMax)` 로 전달 (`bMin > bMax` 검증 + 입력 오류 dialog). **Mode D 사이즈 산정** — `Load > 0` 인 Line 노드마다 `DuctSizingCalculator.ModeD(Load, ductType, α=ModeD_Alpha, bMin, bMax, aspectMax=ModeD_AspectMax)` 호출(`ModeD_Alpha=1.0` / `ModeD_AspectMax=1.5` 만 `DuctTreeBuilder` 내 private const, bMin/bMax 는 파라미터화) → `"a"`(장변)/`"b"`(단변)/`"Disp"=$"{a}x{b}[{Total_CMH}]"` XData 기록 (예: `"600x400[1234.5]"` — `Total_CMH` 와 동일 `"0.##"` 포맷 공유, `totalCmhStr` 로컬 변수에 한 번 계산 후 `Total_CMH` XData 와 `Disp` 양쪽에 재사용). `Total_CMH`/`Tree`/`a`/`b`/`Disp` 모두 **독립 try-catch**. `tr.CheckRegName` 5건 등록 필요. `TTG` 토글 시 Line `Disp` 가 최우선 라벨로 표시(위 `TTG 라벨 우선순위` ⓪).
- **`DuctOutline3` 명령** (2026-06-19 추가, `DuctOutlineCommand.cs`, namespace `PipeLoad2`) — Duct 중심선 3개(메인런 collinear 2 + 수직 분기 1) 선택 → 각 Line XData `"a"`(폭, `SetDuctWidth` 가 기록) 로 **Duct 외곽선(Magenta ACI 6, 현재 레이어, 개별 Line)** 생성. **설계 사양 원본**: `공조덕트사이즈\Duct_OutLine_Design.md` (Step 0~4, 문서가 single source of truth — 코드는 문서 변경 시 재참조). **식별**: 비-collinear = 분기(White); collinear 2 중 **폭 넓은쪽=Green / 좁은쪽=Red**(리듀서는 Red 방향 전이, 색상 아닌 폭으로 판정). 접합점 `O` = 3선 공통 끝점(`TryFindJunction`, tol 1e-3). collinear dot < −0.998, 분기 비90°(|dot|>0.02) → 에러. **생성물(h≠0, 8선)**: 메인런 측벽 4(Green ±hG: `Gfar`→B′, Red ±hR: A→`Rfar`, 끝단 개방·캡 없음) + Reducer 트라페조이드 4(B′ 수직면 wG + 상·하 사선 + A 수직면 wR). 상수 `S=1.5×분기폭`(Stretch), `Arun=h·√3`(=h·tan60°), `h=(wG−wR)/2`. **중심선 조정(원본 변형)**: White 끝점→B(`O+hG·n`), Green 연장→`A_center`(`O+(S+Arun)·u`), Red 단축→`A_center` (`MoveOEnd`: O측 끝점 이동). 동일폭(h=0) → Reducer 생략, 직선 측벽 2선만. `using Autodesk.AutoCAD.Colors;` + `using Color = ...Colors.Color;` 별칭 필요(System.Drawing 모호성). 단일 transaction (LPM/CMH 패턴).
- **`Duct_Outline_Case1` 명령** (2026-06-21 추가, `DuctOutlineCase1Command.cs`, namespace `PipeLoad2`) — **상류 Main(aa) → 하류 Main(bb) 축소 Reducer + 직각 분기(cc)** 외곽선(Magenta ACI 6, 현재 레이어, 개별 Line **13개**) 생성. **설계 사양 원본**: `공조덕트사이즈\Duct_OutLine_case1.md` (Step 1~9, single source of truth). **선택 워크플로우**: `ed.GetEntity`(`PickLine` 헬퍼, Line 만 허용) 3회 — **① 기준선 aa(상류 Main) → ② collinear선 bb(하류 Main) → ③ 수직선 cc(분기)**. 역할은 선택 순서로 고정. 각 Line XData `"a"`(폭)에서 `Wa`(aa)/`Wb`(bb)/`Wc`(cc) 읽기. **접합점 C** = 세 Line 공통 끝점(`TryFindJunction`, tol 1e-3). **로컬좌표**: `u`=C→bb far(Main, aa far는 ≈−u), `v`=C→cc far(Branch). **검증**: aa·bb 반대방향 일직선(dot<−0.999, E05), cc⊥Main(|dot|>0.02, E06), `Wa>Wb>0`·`Wc>0`(E07 동일/확대형 별도 Case), bb 길이≥`S+ww`(E08), cc 길이≥`Ha+hb`(E09), 점 NaN/Inf(E10). **파생치수**: `Ha=Wa/2`, `Hb=Wb/2`, `Hc=Wc/2`, `hb=Wc/4`(Branch 45° 투영), `S=1.5×Wc`(C→CR), `hr=(Wa−Wb)/2`, `ww=hr/tan30°`(CR→CD, Reducer 30°/60°). **생성물 13선**: ①aa상부측벽(`Ea+Ha·v`→J0) ②45°사선(J0→J1) ③cc수평접합(J1→J2) ④cc좌측벽(`Ec−Hc·u`→J1) ⑤cc우측벽(`Ec+Hc·u`→J3, J2지나) ⑥aa상부측벽연장(J3→RTop) ⑦aa하부측벽(`Ea−Ha·v`→RBot) ⑧상부30°사선(RTop→DTop) ⑨하부30°사선(RBot→DBot) ⑩CR수직면(RTop→RBot, Wa) ⑪CD수직면(DTop→DBot, Wb) ⑫bb상부측벽(DTop→`Eb+Hb·v`) ⑬bb하부측벽(DBot→`Eb−Hb·v`). 점: `J0=C−(Hc+hb)u+Ha·v`, `J1=C−Hc·u+(Ha+hb)v`, `J2=C+Hc·u+(Ha+hb)v`, `J3=C+Hc·u+Ha·v`, `CR=C+S·u`, `CD=CR+ww·u`, `R/D Top/Bot=CR/CD±H·v`. 먼 끝(Ea/Eb/Ec)에 캡 없음. **중심선 수정(원본 변형)**: aa·bb 의 C측 끝점만 `CR` 로 이동(`MoveEnd`), cc·모든 XData 불변. 단일 transaction. `using Autodesk.AutoCAD.Colors;` + `using Color = ...Colors.Color;` 별칭 필요. (Case2 와 달리 Reducer 가 있고 분기 1개·collinear 2개 구조 — 혼동 주의.)
- **`Duct_Outline_Case2` 명령** (2026-06-20 추가, `DuctOutlineCase2Command.cs`, namespace `PipeLoad2`) — **T형 양방향 분기** 덕트 외곽선(Magenta ACI 6, 현재 레이어, 개별 Line 11개) 생성. **설계 사양 원본**: `공조덕트사이즈\Duct_OutLine_case2.md` (Step 1~7, single source of truth). **선택 워크플로우(DuctOutline3 와 다름)**: ① `ed.GetEntity`(`AddAllowedClass(typeof(Line))`)로 **기준선 Green 1개 먼저** → ② `JEntity.GetEntityByTpye<Line>` 로 **분기선 Red 2개**. Green/Red 는 선택 순서로 고정(기하 판정 아님). 각 Line XData `"a"`(폭, `SetDuctWidth` 기록)에서 `Wm`(Green)/`Wl`,`Wr`(Red) 읽기. **접합점 C** = Green 한 끝점이 두 Red 와 모두 만나는 점(`TryFindJunction`, tol 1e-3). **검증**: Red 두 선 반대방향 일직선(dot<−0.999, E05), Green⊥Red 90°(|dot|>0.02 → E06), `Wl==Wr`(tol 1e-6, 다르면 E07 별도 Case), 폭>0(E03), Red 길이≥`M+h`(E08). **로컬 좌표**: `u`=C→Gfar(주덕트), `v`=C→Red0 far(분기, Red0=+v / Red1=−v). **파생치수**: `M=Wm/2`, `B=Wb/2`, `h=Wb/4`, `d=200`(고정·폭무관). **생성물 11선**: 분기별(s=±1) 4선[상부벽 `E+B·u`→P1, 목 수직선 P1→P3, 하부벽 `E−B·u`→P4(P3 지나 P4까지), 45° 사선 P1→P2] ×2 + 주덕트 측벽 2(`Gfar±M·v`→Q) + 하부 캡 1(Ql→Qr). 점: `P1=C+s(M+h)v+Bu`, `P2=C+sMv+(B+h)u`, `P3=C+s(M+h)v−Bu`, `P4=C+sMv−Bu`, `Q=C+sMv−(B+d)u`. **중심선 조정(원본 변형)**: Green 의 C측 끝점만 하부 캡 중앙 `Gext=C−(B+d)u` 로 연장(`MoveEnd`). Red 2개·Green far·모든 XData 불변. 단일 transaction(LPM/CMH 패턴). `using Autodesk.AutoCAD.Colors;` + `using Color = ...Colors.Color;` 별칭 필요.
- **`Duct_Outline_Elbow` 명령** (2026-06-29 추가, `DuctOutlineElbowCommand.cs`, namespace `PipeLoad2`) — **동일폭(Wa==Wb) 90° Elbow** 외곽선 생성. 선택: ① aa(Green 기준선) → ② bb(Red 직각선). **접합점 C** = 두 Line 공통 끝점(`TryFindJunction`, tol 1e-3). **로컬좌표**: `u`=C→aa far, `v`=C→bb far. **치수**: `W=Wa`, `H=W/2`, `R=1.5×W`, `Ri=R−H`, `Ro=R+H`. **Arc 기준점**: `Ta=C+R·u`(Green 접선), `Tb=C+R·v`(Red 접선), `O=C+R·u+R·v`(원 중심). **생성물 7개**: Magenta Line 4(`Ea+H·v`→Ai0, Ai1→`Eb+H·u`, `Ea−H·v`→Ao0, Ao1→`Eb−H·u`) + Magenta Arc 2(내측 Ri / 외측 Ro, 모두 `arcStart→arcEnd`) + Green 중심 Arc(R, XData `"a"`=W). **Arc 방향** (`TryArcAngles`): atan2로 startAngle/endAngle 계산 → [0,2π) 정규화 → CCW 포함각 > π이면 start/end 교환 → 세 Arc 동일 각도 재사용. **검증**: E01(동일객체), E02(XData 없음), E03(폭≤0), E04(Wa≠Wb), E05(접합점 없음), E06(90° 아님), E07(길이<R), E08(NaN/Inf), E09(Arc 포함각≠90°). **중심선 수정**: aa C측→Ta, bb C측→Tb. `using System;` 금지(`Autodesk.AutoCAD.Runtime.Exception` 모호성) — `System.Math.*`·`System.Exception` 완전 한정 사용. 단일 transaction. (**설계 사양 원본**: `공조덕트사이즈\Duct_Outline_Elbow.md`)
- **`Duct_C1` 명령** (2026-07-03 신규, `DuctC1Command.cs`, namespace `PipeLoad2`) — **수직 aa + 교차점 X 좌우 직각 bb(좌)/cc(우)** 선택 → aa 덕트 외곽선(Yellow ACI 2, 레이어 `"Duct_OutLine"`, 없으면 자동 생성) 9개 생성 + **bb/cc 를 각각 3개 Line 으로 분리**(bb/cc 외곽선은 생성하지 않음). **설계 사양 원본**: `건축\Duct-OutLine\DuctOutLine_Case_1.md` (v1.4, single source of truth — **`Duct_Outline_Case1`/`DuctOutlineCase1Command.cs` 와 이름이 비슷하나 완전히 다른 형상**이므로 혼동 주의). **선택 워크플로우**: `PickLine` 3회 — ① aa(수직) → ② bb(좌) → ③ cc(우). 각 Line XData `"a"`(폭)에서 `Waa/Wbb/Wcc` 읽기. **교차점 X**: `cc.GetClosestPointTo(aa.StartPoint, true)` (aa⊥cc 이므로 aa 위 임의 점을 cc 연장선에 투영하면 유일한 X). **로컬방향**: `dirAA`=X→aaFar, `dirBB`=X→bbFar, `dirCC`=X→ccFar. **검증**: 세 Line 중복(E01), XData 없음(E02), 폭≤0(E03), aa⊥bb·aa⊥cc 아님(E04), aa/bb/cc 의 X측 끝점이 X 와 불일치(E05), bb/cc 분할점 이후 길이 부족(E06), aa 길이(`y_top`)가 리듀서 사선 높이보다 짧음(E07), NaN/Inf(E08). **파생치수**: `Haa/Hbb/Hcc=W/2`, `Δbb=Wbb/4`, `Δcc=Wcc/4`, **하부 마감 깊이 고정값 `BottomDrop=200`**. **분할점**: `b1=X+dirBB·Haa`, `b2=b1+dirBB·Δbb`, `c1=X+dirCC·Haa`, `c2=c1+dirCC·Δcc` → `SplitBranch` 가 `X→p1`, `p1→p2` 신규 Line 2개(색상/레이어/선종류/XData 원본에서 `Entity.XdataCopy` 로 승계)를 만들고 원본의 X측 끝점만 `p2` 로 이동(`MoveEnd`). **외곽선 9개(v1.4)**: 좌·우 연속 수직선 2개(`y_top`~`−yBot`, 분기 개구에서 끊지 않음) + 하부 수평 1개 + bb/cc 리듀서 각 3개(사선 1 + 끝단 수직 1 + 하단 수평 1, 대칭 사선 없음 — v1.2 대비 형상 변경). `yBot = max(Hbb,Hcc)+BottomDrop`. aa 상단은 캡 없이 열린 상태. 레이어 생성은 `tr.CreateLayer` 확장(이미 있으면 표시만 On). 단일 transaction.
- **`Duct_C2` 명령** (2026-07-04 신규, `DuctC2Command.cs`, namespace `PipeLoad2`) — **동일선상 aa→bb 축소 리듀서 + 상단 직각 cc 분기 컬러(takeoff)**. **설계 사양 원본**: `건축\Duct-OutLine\Duct_OutLine_Case2.md` (v1.5) — **`Duct_Outline_Case2`(T형 양방향 분기)와 이름이 유사하나 완전히 다른 형상, 혼동 주의**. **선택**: ① aa(수평 기준) → ② bb(동일선상) → ③ cc(직각). XData `"a"`로 `Waa/Wbb/Wcc` 읽기, `Waa>Wbb` 필수(E04, 축소 방향). **교차점 X**: `aa.GetClosestPointTo(cc.StartPoint, true)`. **검증**: 중복(E01), XData 없음(E02), 폭≤0(E03), 축소조건(E04), cc⊥aa 아님(E06), aa/bb 반대방향 동일선 아님(E07, `dot+1≈0`), X측 끝점 불일치(E05), bb/cc/aa 길이 부족(E08/E09), NaN(E10). **파생치수**: `hh=(Waa-Wbb)/2`, `Lred=hh/tan30°`(B1-B2), `Lcol=Wcc/4`. **분할·연장점**: `B1=X+dirBB·1.5Wcc`, `B2=B1+dirBB·Lred`, `c1=X+dirCC·Haa`, `c2=c1+dirCC·Lcol`. **처리**: Green(aa) 원본 유지 + 신규 연장 2선(X→B1→B2, aa 승계) / Red(bb) X측 끝점만 B2로 이동(분할 없음) / Cyan(cc) `SplitBranch` 로 3분할. **외곽선 10개**: 본체 상·하 연속 2선(aaFar~B1) + 좌측 마감 수직 1선(T1-U1) + 컬러 3선(45°사선·상단수평·우측수직) + 리듀서 4선(B1수직·상단사선·하단사선·B2수직). 단일 transaction.
- **`Duct_E` 명령** (2026-07-04 신규, `DuctElbowCommand.cs`, namespace `PipeLoad2`) — **직각 엘보**(Green aa 수평 + Red bb 수직) → 코너에 원호 엘보 포함 외곽선. **설계 사양 원본**: `건축\Duct-OutLine\Duct_Elbow.md` (v1.3) — **`Duct_Outline_Elbow`(Magenta, 중심 Arc 포함 7개)와 이름이 유사하나 별개 명령**. **선택**: ① aa(Green) → ② bb(Red). `Waa==Wbb` 필수(E04, 폭 다르면 오류 — Elbow 와 달리 리듀서 결합 없음). **교차점 X**: bb 의 X측 끝점을 aa 에 투영. **치수**: `H=Waa/2`, `R=1.5×Waa`(중심선 반경), `Rin=R-H`, `Rout=R+H`. **기하**: 원호중심 `C=X+dirAA·R+dirBB·R`, 접점 `Tg=X+dirAA·R`, `Tr=X+dirBB·R`. **생성물**: Green측 Line 4개(내측/외측 외곽 2 + Tg/Tr 이음선 2, **Red측 직선 외곽·Green 끝단 마감선은 생성 안 함** — v1.2/v1.3 확정) + 외곽 Arc 2개(내측 Rin/외측 Rout) + 중심선 Arc 1개(반경 R, **aa 의 색상/레이어/XData 승계**). Arc 각도는 `NormalizeAngle` 로 CCW 90° sweep 결정. **기준선 처리**: aa/bb 의 X측 끝점을 각각 Tg/Tr 로 단축(`MoveEnd`). 단일 transaction.
- **`Duct_EE` 명령** (2026-07-05 신규, `DuctEndElbowCommand.cs`, namespace `PipeLoad2`) — **말단 상향 분기(End Elbow)**, 유체 흐름 방향 순서로 a(Green, 흐름 상류) → b(Red, 흐름 하류/분기 상향) 선택. **설계 사양 원본**: `건축\Duct-OutLine\Duct_End_Elbow.md` (v1.1). **교차점 X**: `a.GetClosestPointTo(b.StartPoint, true)`. `dirA=(X-aFar).GetNormal()`(a 의 먼 끝→X, 즉 흐름 방향), `dirB=(bFar-X).GetNormal()`. `Wa`/`Wb` 는 서로 달라도 됨(Case2 의 축소 조건과 달리 폭 대소 제약 없음). **분할·연장점**: `a1=X+dirA·Wb`(a 연장 길이=b 의 폭), `b1=X+dirB·Ha`, `b2=b1+dirB·(Wb/4)`. **처리**: a(Green) 원본 유지 + 신규 연장 1선(X→a1, a 승계) / b(Red) `SplitBranch` 로 3분할(X→b1→b2, b 승계). **외곽선 6개**: 본체 상·하 연속 2선(aFar~a1) + 말단 마감 수직 1선(a1 위치) + 컬러 3선(45°사선 흐름 상류측 1개·상단수평·우측수직, `Duct_C2`/`Duct_OutLine_Case2` 의 8.2 컬러 규칙과 동일 패턴). 단일 transaction.
- **`SetDuctWidth` 명령** (2026-06-19 추가, `DuctTreeCommand.Cmd_SetDuctWidth`) — 선택한 Line 들에 **걸쳐진(straddle) Text(덕트 폭)** 를 읽어 각 Line `"a"` XData 로 **숫자 파싱 없이 TextString 그대로 string 기록** (`"600x400"` 같은 비숫자 허용 → CMH/LPM 의 `double.TryParse` 와 다름). 워크플로우는 `Cmd_Block_SetCMH` 패턴의 Line 버전: `JEntity.GetEntityByTpye<Line>`(LINE 필터, `UsePickSet`) → `tr.CheckRegName("a")` → `ed.ZoomToEntities` → Line 별 Text 매핑 → `ln.UpgradeOpen()` 후 `JXdata.SetXdata(ln, "a", width)`. **걸쳐짐 판정** (private `TryGetWidthTextByLine`): Line bbox(축정렬 Line 대비 길이 5%, 최소 1.0 여유)로 `ed.SelectCrossingWindow`(TEXT/MTEXT 필터) 후보 수집 → 후보 중 **Text 중심의 선분 수선거리**(`Line.GetClosestPointTo(center, false)`)가 최소이고 **Text 높이 이내**인 것 채택. Text 못 찾은 Line 은 Yellow(2) 표시 + 개수 출력. **단일 transaction** (FCUTREE Pre-Transaction 분리 불필요 — LPM/CMH 와 동일). `using Color = Autodesk.AutoCAD.Colors.Color;` 별칭 필요(System.Drawing.Color 모호성). `"a"` 만 기록(`Disp`/`b` 미기록 → `TTG` 라벨 영향 없음).

## 파일/폴더
- 프로젝트 루트: `C:\Users\junhoi\Desktop\Work\Acadv25JArch\Acadv25JArch\`
- 솔루션: `C:\Users\junhoi\Desktop\Work\Acadv25JArch\Acadv25JArch.sln`
- 프로젝트 문서/노트는 Obsidian에 기록 (본 디렉토리에 .md 두지 말 것)
- **`PipeDiaCalc/`** — LineTree / FcuLineTree / DuctTree / DiaNote 관련 소스 (관경계산·치수기입·덕트분석 파이프라인):
  - `namespace PipeLoad2`: `LineTreeBuilder.cs` (+ `PipeLoadAnalysis` 클래스: `PPL`/`LPM`/`CMH`/`LineExtend2Block` 명령), `LineTreeForm.cs/.Designer.cs/.resx`, `LineTreeFormCommand.cs`, `FcuLineTreeBuilder.cs`, `FcuLineTreeForm.cs/.Designer.cs/.resx`, `FcuLineTreeFormCommand.cs`, `DuctTreeBuilder.cs`, `DuctTreeCommand.cs`(+`Cmd_SetDuctWidth`/`SetDuctWidth` 명령), `DuctTreeForm.cs/.Designer.cs/.resx` (2026-05-14 신규), `DuctOutlineCommand.cs`(`DuctOutline3` 명령 — 2026-06-19 신규), `DuctOutlineCase2Command.cs`(`Duct_Outline_Case2` T형 분기 외곽선 — 2026-06-20 신규), `DuctOutlineCase1Command.cs`(`Duct_Outline_Case1` Reducer+분기 외곽선 — 2026-06-21 신규), `DuctOutlineElbowCommand.cs`(`Duct_Outline_Elbow` 동일폭 90° Elbow 외곽선 — 2026-06-29 신규), `DuctC1Command.cs`(`Duct_C1` 수직 aa 외곽선+bb/cc 3분할 — 2026-07-03 신규), `DuctC2Command.cs`(`Duct_C2` aa→bb 축소+cc 분기 컬러 — 2026-07-04 신규), `DuctElbowCommand.cs`(`Duct_E` 직각 엘보 Arc — 2026-07-04 신규), `DuctEndElbowCommand.cs`(`Duct_EE` 말단 상향 분기 — 2026-07-05 신규)
  - `namespace Acadv25JArch`: `DiaNote.cs` (`DiaNote` 클래스 — `cmd_DiaTree`/`cmd_DiaNoteVer[1]`/`cmd_DiaNoteHor[1]`/`Cmd_SetDiaNoteBase`/`Cmd_SetDiaNoteBaseText`/`DD` 명령 + `SusPipe` 클래스 — `susDia`/`susDia1` 명령)
  - `namespace Acadv25JArch.PipeDiaCalc`: `AinitCommand.cs` (`Ainit` 명령 — NamedObjectsDictionary `AINIT_DEFAULTS` 사전에 DwgDefault static 값을 Xrecord로 저장; `Application` 모호 참조 회피용 `using Application = Autodesk.AutoCAD.ApplicationServices.Application;` 필수)
  - csproj `<Compile Update>` / `<EmbeddedResource Update>` 경로는 `PipeDiaCalc\...` 사용 (DependentUpon 은 basename만)
- **`Util_Command.cs`** (프로젝트 루트, `namespace PipeLoad2`, `CommandUtil` 클래스) — Line/Arc/Polyline 선택·분할 유틸 모음. Duct/Pipe 파이프라인과 독립된 범용 유틸(외부 유틸 의존 없는 신규 시스템). 사양 문서는 `Acadv25JArch\Util_Command\`(솔루션 루트) 폴더에 별도 보관:
  - `CSS`(`DuctPipe_SelectChain`) — Duct/Pipe 계열 XData(`Duct,Pipe,MainPipe,FirePipe`) 가진 Line/Arc/LWPolyline 체인을 끝점 연결 기준으로 추적·선택. `SetImpliedSelection`은 명령 종료 후 첫 Idle 시점에 실행(`_pendingSelectIds` + `Application.Idle += OnIdleSetSelection`) — 명령 내 직접 호출 시 선택이 무효화되는 문제 회피. (`CSS.md` v1.1)
  - `ss2`(`MultiLineSplit_v2`) — Base Line 교차 분할 + Target Line 끝점 스냅. (`SS1.md` v1.2)
  - `To_Duct`(`SetXdataToDuct`) — 선택 Entity에 RegApp `"Duct"` XData 일괄 지정(`EnsureRegApp`로 RegAppTable 등록 확인 후 기록).
  - `SCL`(`SelectConLine`, 2026-07-07 신규) — 기준 Line + 클릭점(진행 방향)으로 연결된 Line을 연속 선택. 끝점 연결 우선 추적, 없으면 진행 방향 앞쪽 collinear Line으로 점프(최대 900, 최근접 1개). **`SetImpliedSelection`을 명령 내에서 직접 호출** — `CSS`가 겪었던 것과 같은 무효화 문제가 재현될 수 있어 실사용 확인 필요(`SCL.md` v1.0 §6 참고).
- **`Overrule/`** — DrawableOverrule 기반 시각화 명령 모음 (2026-04-27 분리):
  - `TreeOverrule.cs` (`namespace PipeLoad2`) — `TTG` 명령. ① Line XData `"Tree"` (Root/Mid/Leaf) 기반 색상/LineWeight 오버레이 + 라벨. ② BlockReference XData `"Disp"` 기반 geo 센터 Red 텍스트(짧은변/2 크기). 두 인스턴스로 분리 등록 (각자 `SetXDataFilter`). `FcuLineTreeBuilder.ApplyDiameters` / `LineTreeBuilder.ApplyDiameters` / `Cmd_Block_SetLPM` 이 저장한 XData 전제.
  - `ArchOverrule.cs` — `aag`/`aag3` (Wire Graphic), `REGISTERXDATAFILTER`/`UNREGISTERXDATAFILTER`, `ADDARCHXDATA`/`ADDARCHXDATABATCH`/`REMOVEARCHXDATA`, `TESTXDATAFILTER`. `XDataFilterDrawOverrule` 패턴은 `TreeOverrule.cs` 의 참고 원본.
- **`Ribbon/`** — `Autodesk.Windows` 기반 커스텀 리본 (2026-04-30 추가):
  - `CollabRibbon.cs` (`namespace Acadv25JArch.Ribbon`) — JArch 탭 (Id `ACADV_COLLAB_JARCH`). 4개 패널(DiaNote/Autodesk Docs/트레이스/비교). DiaNote 패널: Row 1 LargeButton 2개 (`"DiaNote 높이 변경"` / `"SET"`, 둘 다 `Cmd_SetDiaNoteBase` 호출) + Row 2 `"크기: <값>"` 표시. 가시성은 `AINIT_DEFAULTS` 사전 존재 여부에 연동 (`SetRibbonTabVisible`). `DiaNote.BaseLen` 변경 시 `RefreshDiaNoteHeight()` 로 라벨 갱신 — `Ainit` / `LoadDwgDefaults` / `SaveBaseLen` 직후 호출.
- **`PaletteSample.cs`** (`namespace PaletteSample`, 2026-05-01 추가) — `SHOWPAL`/`HIDEPAL` 도구 팔레트 샘플. `LayerManagerPalette`와 동일한 lazy init 패턴(첫 명령 호출 시 `PaletteHost.EnsureCreated`). `MyToolsControl : UserControl` — `FlowLayoutPanel` 가로 + 그룹별 세로 스택. 버튼 `Tag`에 명령 문자열(`"_LINE "` 등) 저장 → Click 시 `doc.SendStringToExecute`. **주의: `[assembly: ExtensionApplication]` / `[assembly: CommandClass]` 속성은 `MyPlugIn.cs`와 충돌하므로 절대 추가 금지.**
